// Função PAR ou IMPAR

/*
function parImp(n) {
  if (n % 2 == 0) { //Ação e abaixo os possíveis retornos
    return `O número ${n} é par!`;
  } else {
    return `O número ${n} é ímpar!`;
  }
}
let res = parImp(0) //Chamada

console.log(res)


// Função SOMA

function soma(n1, n2){
  return n1 + n2
}
console.log(soma(2,2))

//Variável que recebe função
var v = function(x) {
  return x*2
}
console.log(v(5))



function fatorial(n) {
  let fat = 1;
  for (let c = n; c > 1; c--) {
    fat *= c;
  }
  return fat;
}
console.log(fatorial(5));

//5! = 5 x 4 x 3 x 2 x 1

*/
